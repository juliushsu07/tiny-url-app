<!DOCTYPE html>
<html lang="en">

<head></head>

<body>
    <% include ./partials/_header %>
        <!-- Add your EJS code here -->
        <ul>
        <% for(var url in urls){  %>
            <li>
            <%= url %> : <%= urls[url].longURL %>
              <form method="POST" action="/urls/<%= url %>/delete">
                  <button>delete</button>
                  <a href="/urls/<%= url %>"> Edit </a>
              </form>
            </li>

        <% }                       %>
        <a href="/urls/new">Create a new short link</a>
        </ul>
</body>

</html>